spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/tardis_db
    username: postgres
    password: ***REMOVED***
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
