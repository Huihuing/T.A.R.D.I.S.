# Flyway migrations

이 디렉터리는 검증된 DB migration SQL만 저장합니다.

현재 운영 Aiven MySQL 스키마의 schema-only dump를 아직 확정하지 않았으므로
추측한 V1 baseline SQL은 의도적으로 커밋하지 않습니다.

baseline을 만들기 전에는 docs/DB_MIGRATION.md의 절차를 따르세요.
